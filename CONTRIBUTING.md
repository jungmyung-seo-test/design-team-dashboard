# 이 보드를 함께 고치려면

리포 협업자로 추가된 분을 위한 문서다. 전체 구조는 [README](README.md),
처음부터 세우는 절차는 [ci/SETUP.md](ci/SETUP.md) 에 있다.

---

## 0. 먼저 알아야 할 것

**이 보드에는 구성원 실명 26명, 개인별 업무량과 과부하 판정, 사내 지라 이슈
제목이 들어 있다.** 리포 자체에는 개인정보가 없지만(전부 시크릿과 빌드 산출물로
분리해 뒀다), 다루는 대상이 그런 데이터라는 것을 전제로 작업한다.

- 보드는 **커밋하지 않는다.** Actions 가 만들어 Pages 로 바로 배포한다
- `ci/BOARD_CONFIG.json` (명부) 은 **절대 커밋하지 않는다.** `.gitignore` 가 막고
  있지만, `git add -f` 같은 것으로 우회하지 말 것
- Actions 로그는 공개 리포라 누구나 본다. 그래서 스크립트가 실명을 가려 찍는다
  (`김*아`). 디버깅한다고 `BOARD_VERBOSE` 를 켜서 푸시하지 말 것

## 1. 시작하기

### 준비물

| | 용도 |
|---|---|
| git | 클론·푸시 |
| **Node 18 이상** | 로컬 렌더 확인 (`node -v` 로 확인) |
| 브라우저 | 렌더 결과 확인 |
| Claude Code (선택) | 작업 보조 |

Python 은 로컬에서 쓸 일이 거의 없다. 집계는 CI 에서만 돈다.

### 1) 클론

```bash
git clone git@github.com:jungmyung-seo-test/design-team-dashboard.git
cd design-team-dashboard
```

SSH 키가 없으면 HTTPS 로 받아도 된다.
`git clone https://github.com/jungmyung-seo-test/design-team-dashboard.git`

### 2) 명부 파일 받아 두기

`ci/BOARD_CONFIG.json` 을 **리포 오너에게 따로 받아** 그 경로에 둔다.
구성원 명부라 리포에는 없다. `.gitignore` 가 커밋을 막고 있으니 그대로 두면 된다.

### 3) 보드 비밀번호 받아 두기

로컬 렌더 확인에 필요하다. 팀에 공유된 그 비밀번호다.

### 4) Claude Code 로 시작한다면

**이 리포 폴더 안에서** 실행한다.

```bash
cd design-team-dashboard
claude
```

Claude 계정이 회사 메일이든 개인 메일이든 상관없다. 푸시는 Claude 계정이 아니라
**그 컴퓨터의 git 자격증명**으로 나가므로, GitHub 협업자로 추가돼 있으면 된다.

첫 대화는 이렇게 시작하면 맥락을 빨리 잡는다.

> README.md 와 CONTRIBUTING.md 를 읽고 이 보드가 어떻게 만들어져서
> 어떻게 배포되는지 정리해줘. 특히 업무 유형 분류 규칙과 푸시 후 반영 절차를.

---

## 1-1. 한 번 해보기 — 전형적인 작업 흐름

화면 문구 하나를 고친다고 하자.

```bash
# 1) 고친다
#    ci/build/shell.html 에서 해당 문구를 찾아 수정

# 2) 화면이 실제로 그려지는지 확인 (필수)
BOARD_PASSWORD='비밀번호' node ci/rendertest.mjs
open _rendertest.html
#    → 카드가 인원수만큼 보이는가 · 카드를 눌러 패널이 열리는가
#    → 개발자도구 콘솔에 오류가 없는가

# 3) 푸시
git add -A && git commit -m "..." && git push

# 4) 30초쯤 기다렸다가 보드에서 '지금 갱신' 클릭
#    바로 누르면 Actions 가 직전 커밋으로 체크아웃하는 경우가 있다

# 5) 40초쯤 뒤 보드를 새로고침해 확인
```

**2번을 건너뛰지 않는다.** 이유는 3번 항목에 적어 뒀다.

## 2. 무엇을 고치게 되나

| 고치려는 것 | 파일 |
|---|---|
| 화면·레이아웃·상호작용 | `ci/build/shell.html` (템플릿 + CSS + JS 전부) |
| 업무 유형 분류, 부하 계산, 집계 | `ci/build/agg.py` |
| 지라 조회 조건 | `ci/build/fetch.py` · `BOARD_CONFIG` 시크릿 |
| 게시 전 검증 | `ci/build/verify.py` |
| 갱신 버튼 서버 | `ci/worker/index.js` (배포는 오너만) |

명부·소속·ONE 그룹은 **코드가 아니라 `BOARD_CONFIG` 시크릿**에 있다.
인원이 바뀌면 그 시크릿만 고친다.

## 3. 푸시 전에 반드시 — 로컬 렌더 테스트

**`shell.html` 을 고쳤으면 화면이 실제로 그려지는지 먼저 확인한다.**
문자열을 세는 것으로는 부족하다. 실제로 이 단계를 건너뛰어 보드가 통째로
비어버린 적이 있다 — JS 템플릿 리터럴 안에 HTML 주석을 넣었더니 `${}` 가
그대로 평가돼 `ReferenceError` 가 났고, 문법 검사도 통과했다.

```bash
BOARD_PASSWORD='팀에 공유된 비밀번호' node ci/rendertest.mjs
```

Node 내장 모듈만 쓰므로 설치할 것은 없다. 만들어진 `_rendertest.html` 을
브라우저로 연다 (`open _rendertest.html`).

배포된 보드에서 데이터만 빼내 지금 고친 `shell.html` 에 넣는다. 지라 조회도
CI 도 필요 없다. **카드가 인원수만큼 보이는지, 카드를 눌러 패널이 열리는지,
콘솔에 오류가 없는지** 확인한다.

## 4. 반영 절차

```bash
git add -A && git commit && git push
```

푸시만으로는 보드가 안 바뀐다. 갱신이 돌아야 새 코드로 다시 만들어진다.

- **푸시하고 30초쯤 기다렸다가** 보드의 `지금 갱신` 을 누른다.
  바로 누르면 Actions 가 직전 커밋으로 체크아웃하는 경우가 있다
- 갱신은 40초쯤 걸린다 (지라 조회 17초 + 빌드 + 배포)
- 안 바뀐 것 같으면 한 번 더 누른다

## 5. 검증이 막으면 배포되지 않는다

`ci/build/verify.py` 가 게시 전에 확인한다. 하나라도 어긋나면 **배포하지 않고
옛 보드가 그대로 남는다.** 반쪽짜리로 덮어쓰는 것보다 낫다는 판단이다.

- 명부 26명이 `LOAD`·`DESIGN`·`ACCT` 에 모두 있는지
- 업무 유형이 템플릿의 `WTYPES` 와 일치하는지
- 유형 합계 = 상태 합계 = 목록 총계인지
- **탭 수와 뷰 섹션 수가 서로 맞는지** (매직 넘버를 쓰지 않는다. 예전에
  "탭 3개" 고정값을 검사하다 구조 변경 후 매번 게시가 거부돼 보드가 5일 멈췄다)

Actions 실행 로그의 `검증` 단계를 보면 무엇이 걸렸는지 나온다.

## 6. 자주 밟는 함정

| 증상 | 원인 |
|---|---|
| 카드가 안 보임 | 템플릿 리터럴 안에서 없는 변수를 참조. 3번 렌더 테스트로 잡는다 |
| 카드 숫자 ≠ 목록 총계 | 두 곳이 다른 배열에서 셈. 같은 배열에서 세야 한다 |
| 고친 게 반영 안 됨 | 푸시 직후 바로 갱신. 30초 두고 다시 |
| 분류가 이상함 | 티켓의 `rk`(최상위 조상) · `kt`(KTLO 근거) 를 본다. README 참고 |
| 로컬에서 `agg.py` 가 안 돎 | 지라 원본(`raw/`)이 필요하다. 집계는 CI 에서만 돈다 |

## 7. 하지 말 것

- `BOARD_CONFIG.json` · 빌드 산출물 커밋
- 워크플로에서 시크릿 값을 출력하는 코드 (로그가 공개다)
- 검증(`verify.py`) 을 우회하거나 조건을 느슨하게 바꾸기
- 렌더 테스트 없이 `shell.html` 푸시
